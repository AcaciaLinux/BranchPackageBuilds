name=leaf
version=0.2.2
real_version=0
source=
dependencies=[libarchive][curl][libxml2]
builddeps=[libarchive][curl][libxml2][git][certificate-ca][cmake]
description=Leaf is the package manager for AcaciaLinux
build={
	echo "nameserver 1.1.1.1" > /etc/resolv.conf

	git clone --recurse-submodules --branch=0.2.2 https://github.com/AcaciaLinux/leaf
	cd $PKG_NAME

	mkdir build
	cd build

	cmake ..
	make -j $(nproc)
	make DESTDIR=$PKG_INSTALL_DIR install
}